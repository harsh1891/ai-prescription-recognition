# Models

Place optional local model artifacts here, such as fine-tuned BioBERT, Med7, or layout-aware OCR checkpoints.

The default app uses hosted VLM providers plus deterministic mock mode, so this directory can stay empty during early development.

